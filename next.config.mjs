/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: [], // Add allowed domains for external images here if needed
  },
};

export default nextConfig;
